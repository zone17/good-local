A round icon-only tap target. `size="lg"` gives 52px for outdoor / wet-hand use.

```jsx
<IconButton label="Back" onClick={...}><Icon name="arrow-left"/></IconButton>
<IconButton size="lg" bordered label="Close"><Icon name="x"/></IconButton>
```
