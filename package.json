{
  "name": "good-local-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run tests/integration",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49",
    "@supabase/supabase-js": "^2",
    "@types/node": "^20",
    "dotenv": "^16",
    "typescript": "^5",
    "vitest": "^2"
  }
}
