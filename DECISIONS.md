# Good Local — Architectural Decision Log

> Index of binding decisions. Detail lives in the attestation chain (`docs/`, `specs/`,
> `.specify/memory/constitution.md`) — entries here are pointers, not duplicated prose
> (Constitution Art. XXXII).

| ID | Date | Decision | Status | Detail |
|----|------|----------|--------|--------|
| D-001 | 2026-06-06 | **Mechanism: two-loop regional passport, not a points coalition.** Depth = same-business repeat visits; breadth = season-cumulative regional progress. Shared currency demoted to a gated Phase-2 experiment. | Adopted (verified) | `docs/discovery/02-opportunity-map.md`, `04-verification-verdicts.md` P1/P5 |
| D-002 | 2026-06-06 | **Pricing: $79/mo from day one** (founder decision, overriding the free-season recommendation); founding-rate lock; $49 winter tier Nov–Apr. | Adopted (founder) | `04-verification-verdicts.md` addendum; PR/FAQ v2 internal FAQ |
| D-003 | 2026-06-06 | **Standalone-first architecture** — every business's program fully functional with zero other participants; network compounds on top. | Adopted (founder) | PR/FAQ v2; Constitution Art. IV |
| D-004 | 2026-06-06 | **Check-in trust model is constitutional**: rotating codes + server-side validation + 1/patron/business/day + two attribution paths only; unverifiable check-ins void all gates. | Adopted | Constitution Art. II; spec FR-013–018 |
| D-005 | 2026-06-06 | **Patron privacy boundary enforced at domain layer**: owners see own-business aggregates only; cross-business history never exposed. | Adopted | Constitution Art. V; spec FR-029 |
| D-006 | 2026-06-06 | **Stamps carry no monetary value** (closed-loop NY/PA posture); change requires legal review + constitutional amendment. | Adopted | Constitution Art. VI |
| D-007 | 2026-06-06 | **The launch is pre-registered experiment A1** — gates, thresholds, kill condition (Aug 15), sample floor (Jul 31), retention read (Nov 1) are binding; instrumentation is launch-blocking. | Adopted (verified) | `docs/discovery/00-discovery-brief.md`; Constitution Art. III |
| D-008 | 2026-06-06 | **Design system vendored at `design/` as canonical** (Claude Design handoff): pine/stamp/ochre/river/paper tokens, Newsreader + Public Sans + DM Mono, round NP stamps, pine-leather pass, Stamped Moment icon. Product surfaces consume, never fork. | Adopted | `design/README.md`; Constitution Art. VIII; skill `.claude/skills/good-local-design` |
| D-009 | 2026-06-06 | **Frontend stack: Vite + React, two runtime deps**, mobile-web-first; `app/src/data.js` is the single mock data seam to swap for the API. Distribution issuance (wallet-pass-first vs home-screen) is an open A/B resolved in the pre-launch landing test. | Adopted (A/B open) | `app/README.md`; `04-verification-verdicts.md` P7 |
| D-010 | 2026-06-06 | **Domain hierarchy: Region → Town → Business; Patron outside the tree.** Passports, seasons, milestones, picks are region-scoped; v1 ships one region as data; no multi-region infrastructure before region #2 exists. | Adopted | spec Key Entities + FR-035; Constitution Art. XVI |
| D-011 | 2026-06-06 | **Constitution v1.0.0 ratified** — modeled on the Diamond Ledger agent-native constitution; product-integrity Articles I–IX rebuilt for Good Local; Articles I, II, V, VI have no exception path short of amendment. | Ratified | `.specify/memory/constitution.md` |
| D-012 | 2026-06-06 | **Spec Kit drives delivery** — constitution → specify → plan → tasks → implement on `NNN-feature` branches; the holistic spec is bound to the attestation chain (conflicts resolve toward attestations). | Adopted | `specs/001-upper-delaware-passport/spec.md` |
| D-013 | 2026-06-06 | **Interim document styling** — Good Vibes Coding design system used strictly for docs/attestation HTML (self-contained files via `docs/assets/gvc/inline.py`), never for the product. | Adopted | `docs/assets/gvc/` |
| D-014 | 2026-06-06 | **`qrcode` added as app dependency #4 (US1 T023)** — the register kit prints a real, scannable QR of the check-in `qr_url`; honest print reliability for rural/wet-hand use rules out an external QR API (offline/print durability) and a hand-rolled encoder. Well-maintained, dependency-light; bundled in the main entry only (the lean check-in entry is unaffected, still ~0.36 KB gz). | Adopted | `app/src/business/RegisterKit.jsx`; `app/package.json`; supersedes D-009 "two runtime deps" count |
| D-015 | 2026-06-06 | **`@axe-core/playwright` added as a dev dependency (T060)** — the accessibility e2e asserts WCAG AA + tap-target floors (Art. IX) mechanically rather than by spot-check; axe-core is the standard engine and runs inside the existing Playwright projects, adding no runtime weight. Dev-only; does not touch either entry's budget. | Adopted | `package.json` devDeps; `tests/e2e/a11y.spec.ts` |
| D-016 | 2026-06-06 | **Vitest `globalSetup` runs `supabase db reset` once per invocation** — suites legitimately mutate shared seed fixtures (rename/suspend the demo business, redeem its perk); per-suite restore proved order-fragile (green from fresh seed, rotting on accumulated state). One reset per run makes local identical to CI (which resets per job) and kills the flake class. `SKIP_DB_RESET=1` opts out for single-file iteration; a `/tmp/gl-db-lock` mutex coordinates with implementation agents. | Adopted | `tests/setup/global-setup.ts`; `vitest.config.ts`; `docs/solutions/shared-seed-fixture-rot.md` |
| D-017 | 2026-06-06 | **Shared `gl-auth` storage key unifies the two-entry identity** — the main bundle and the lean check-in bundle both build the Supabase client with `storageKey: "gl-auth"` + full session persistence + refresh, so one anonymous session spans both entries. Separate storage would fork a first scan and the passport home into two anonymous patrons. | Adopted | `app/src/lib/auth.js`; `app/src/checkin/checkin-api.js`; `docs/solutions/split-bundle-identity-fork.md` |
| D-018 | 2026-06-06 | **Perk progress is "since last redemption", defined once in SQL** — a patron's progress for a perk is the count of trust-valid, non-voided, current-season stamps at that business created strictly after their most-recent redemption of that perk (full season count if never redeemed). `perk_progress_count()` is the single source; `checkin_progress` (0007) replaces the 0006 all-stamps count so check-in confirmation, passport, dashboard, and redeem all agree and progress resets to 0 after a redemption. | Adopted | `supabase/migrations/0007_rpc_redeem.sql`; `docs/solutions/postgres-since-last-redemption-progress.md` |
| D-019 | 2026-06-07 | **`get_business_regulars` added to the contract (§3.10, migration 0013)** — removing the mock seam (T064) exposed the Regulars view had no backing verb. Owner-scoped per Art. V (own-business rows only); `display_name` nullable — honest fallback, never invented names. | Adopted | `specs/.../contracts/api.md` §3.10; `supabase/migrations/0013_rpc_regulars.sql`; `tests/contract/regulars.test.ts` |
| D-020 | 2026-06-07 | **Contract drift closed: `add_wallet_pass` contracted (§2.7) + tested; `list_pending_businesses` promoted to a full §4.1 entry; §3.9 aligned to the edge-only implementation; verb count 27→29.** The arch audit found `add_wallet_pass` (migration 0006) implemented, granted, and writing the `passport_adds` gate input with no contract entry and no contract test — the same class as D-019. Standard reaffirmed: a verb ships contract-first or gets contracted + tested the moment it's found. | Adopted | `specs/.../contracts/api.md` §2.7, §4.1, §3.9; `tests/contract/wallet-pass.test.ts` |
| D-033 | 2026-06-11 | **Launch hardening wave 1 (spec 002 US-foundational + quick wins).** (a) Runtime safety net: ErrorBoundary + one-shot stale-chunk reload (`lib/recover.jsx`) wraps both entries; check-in fetches get 15s timeouts and the error screen gets a real "Try again" (server-side stamp commit makes retry DAILY_LIMIT-safe). (b) Error seam fixed (audit API-001): `toApiError` maps P0001 message-carried §7 codes; `edge()` parses the `FunctionsHttpError` body — per-code UI copy now actually fires; regression-tested in `tests/unit/error-seam.test.ts`. (c) False-success bugs closed: winter-tier swap hard-fails on any skipped Stripe call (ERR-011), `shareWeeklyNote` invokes the delivering edge fn (ERR-012), OTP send returns new §7 `SMS_UNAVAILABLE` instead of `sent:true` when no provider, with masked phone + no code in logs (ERR-009/COMP-004). (d) Honest-copy pass: wallet CTAs → "Save my passport", staff claim-link + patron by-text promises rewritten, fake privacy toggles + mock email deleted, same-day promise aligned, printed-kit line fixed client + server side. (e) Perimeter: vercel.json CSP/XFO/nosniff/Referrer/Permissions headers + immutable asset caching + `npm ci`; migration 0018 adds hot-path indexes, audit-table DELETE revokes, 30-day OTP purge cron. SMS consent line added at both phone inputs; `tsconfig allowJs` for JS-seam tests; dead `PERKS`/`BUSINESS` fallbacks and the mail-me stub removed. | Adopted | `specs/002-launch-hardening/`; `supabase/migrations/0018_launch_hardening.sql`; `app/src/lib/recover.jsx`; `tests/unit/{otp-core,error-seam}.test.ts`; contract §7 `SMS_UNAVAILABLE` |
| D-032 | 2026-06-11 | **Production readiness audit (14-domain) + GTM/launch-film strategy adopted as the post-launch-prep backlog.** Audit verdict RED: 3 P0s (backups unverified, no error boundary, no privacy policy), ~45 unique P1s across observability (zero telemetry), compliance (no legal surface, TCPA consent gaps), error handling (silent false-success on winter-tier swap, weekly-note share, OTP send), SEO (marketing routes invisible to crawlers/scrapers), and pipeline (unprotected main, manual DB deploys). Remediation is spec-driven: `002-launch-readiness-hardening` (Waves 0–2) and `003-marketing-discoverability` (SEO foundation + content engine). GTM: density-first two-sided launch per the PR/FAQ; launch film via Veo 3.1 primary / Runway Gen-4.5 control layer / Kling-Seedance cutdowns, with the authenticity guardrail (real footage for real claims, AI for atmosphere only; Sora 2 discontinued 2026-04-26 and excluded). | Adopted | `docs/audits/production-readiness-2026-06-11.md`; `docs/marketing/gtm-launch-strategy.md` |
| D-031 | 2026-06-11 | **Pinned the CI Supabase CLI to 2.105.0.** CI used `setup-cli` `version: latest`; **CLI v2.106.0 (published 2026-06-11)** changed `service_role` grant behavior so the local stack's `db reset` finished cleanly but the service role lost SELECT on core tables — every contract/e2e suite failed with "permission denied for table seasons/patrons", with no code change. Pinned both CI jobs to **2.105.0** (the last `latest` across all our green runs, published 2026-06-04). Revisit before bumping; the project's own migrations may need explicit `service_role` grants if a future CLI keeps this behavior. | Adopted | `.github/workflows/ci.yml` |
| D-030 | 2026-06-11 | **Marketing review backlog cleared (P2/P3).** Content hooks now return `{ ..., error }` so Blog/Podcast show a "couldn't load" state distinct from a genuinely empty list. Mobile menu: Escape closes + returns focus to the burger, and it closes on resize past the 760px breakpoint (e2e in `landing-nav.spec.ts`). Fonts: confirmed `display=swap`, added preconnect + a parse-time stylesheet `<link>` in index.html (earlier LCP); title/description rewritten dash-free to match the new positioning. Markdown `_italic_` no longer fires inside `snake_case`; hero `WalletPass` capped at `min(300px, 82vw)` (no narrow overflow); `:focus-visible` outline added for marketing links + the burger. DS barrel verified tree-shaking clean (Landing chunk has no Tabs/Input/StampGrid) — no `sideEffects` change needed. | Adopted | `app/src/marketing/useContent.js`, `Blog.jsx`, `Podcast.jsx`, `Chrome.jsx`, `Landing.jsx`; `app/index.html`; `tests/e2e/landing-nav.spec.ts` |
| D-029 | 2026-06-10 | **"Bring Good Local to your region" demand capture.** The movement section's CTA was a dead link; replaced with a real lead-capture form (town/region + email) writing to a new `region_interest` table (migration 0017). RLS: anyone may INSERT (public capture), only `is_admin()` may SELECT; column CHECKs bound field sizes and validate the email shape so the open insert endpoint can't be junk-filled. The form posts via a **bare fetch against PostgREST, not the Supabase SDK**, so the landing stays SDK-free (preserves D-028). A new "Regions" admin view lists requests with per-region demand counts. E2E (`region-interest.spec.ts`) covers the submit flow + asserts anon cannot read the table back. | Adopted | `supabase/migrations/0017_region_interest.sql`; `app/src/marketing/RegionInterestForm.jsx`, `regionInterest.js`, `Landing.jsx`; `app/src/admin/AdminApp.jsx`; `tests/e2e/region-interest.spec.ts` |
| D-028 | 2026-06-10 | **Supabase client split out of the main entry (P1 perf from the marketing review).** `App.jsx` statically imported `lib/auth.js`, so the full Supabase SDK (~55 KB gz) shipped in the main entry and downloaded on the landing — the most-hit route — which never uses it. Extracted the inline `/admin` gate into a lazy `admin/AdminRoute.jsx` so `App.jsx` imports no auth; the SDK now lives only in the lazy admin/app chunks. **Main entry dropped 101 → 46.7 KB gz.** Landing/admin/patron verified rendering with zero page errors. | Adopted | `app/src/App.jsx`; `app/src/admin/AdminRoute.jsx` |
| D-027 | 2026-06-09 | **Anchor-linked landing sections offset for the sticky header.** Clicking an in-page nav link (#how, #business) scrolled the section flush to the viewport top, where the sticky header clipped its heading. Added `scroll-margin-top: 84px` to the shared `Section` so anchored sections land below the header. Added `tests/e2e/landing-nav.spec.ts` asserting the target heading's top sits at or below the header's bottom edge (regression guard). | Adopted | `app/src/marketing/Landing.jsx`; `tests/e2e/landing-nav.spec.ts` |
| D-026 | 2026-06-09 | **Landing reframed: Good Local is the platform; the Upper Delaware is region one of many.** The first landing read as an Upper-Delaware-only app; repositioned so a visitor grasps that Good Local is a regional-passport movement that begins on the Upper Delaware and spreads ("region one of many", "your region could be next") — added a dark-pine "The movement" section. Copy rewritten by a 3-voice copywriter panel (brand-storyteller / direct-response / minimalist) synthesized into one deck: hero "The places worth coming back to.", dual-audience subhead, no-paid-placement trust, the $79 owner pitch, region-expansion CTA. House style for the marketing surface: **no dashes of any kind** (em/en/hyphen-as-punctuation) — enforced by self-scan, e.g. "river town residents" not "river-town". Grounded in the PR/FAQ + discovery brief. Budgets/brand still green; a11y landing assertion updated to the new headline. | Adopted | `app/src/marketing/Landing.jsx`, `Chrome.jsx`, `Blog.jsx`; `tests/e2e/a11y.spec.ts` |
| D-025 | 2026-06-09 | **Public marketing surface: landing + blog + podcast; patron app moves to `/app`.** The root `/` served the patron passport, so business owners hitting goodlocal.app saw a consumer app with no explanation — a funnel gap once registration went live. New routes: `/` = marketing landing (hero, how-it-works, $79 business pitch, no-paid-placement trust, blog/podcast teasers, CTAs to /app + /business/signup), `/blog` + `/blog/{slug}` = weekly review, `/podcast` = episodes. Patron app now at `/app`; the 3 check-in "Open my passport" links repoint there; QR `/c/` unchanged. **CMS = Supabase, not an external vendor**: `blog_posts` + `podcast_episodes` tables (migration 0016, RLS: anon reads published, is_admin() writes), authored from a new "Content" section in `/admin` (browser editing, no new login/cost). Podcast audio hosted externally; we store embed/listen URLs only. Markdown rendered by a tiny dependency-free escape-first renderer (admin-only authored + hrefs constrained to http(s) → XSS-safe, no DOMPurify/bundle cost). All route components lazy-loaded so the landing ships ~3 KB + the main entry dropped to 100.8 KB gz (from 124). | Adopted | `app/src/marketing/`; `app/src/App.jsx`; `app/src/admin/AdminApp.jsx`; `supabase/migrations/0016_content_blog_podcast.sql` |
| D-024 | 2026-06-08 | **Passport home renders for a never-scanned patron (empty state, not UNAUTHENTICATED).** Live smoke test surfaced it: a first-time visitor opens the app before scanning, so the anonymous session exists (`auth.uid()` set) but the `patrons` row is only created lazily on first check-in — `current_patron_id()` was null and `get_my_passport` raised UNAUTHENTICATED, leaving the home stuck (the hook rendered the error state identically to "Loading…"). Worked locally only because test patrons already had activity. Fix: migration 0015 splits the null cases — no session → UNAUTHENTICATED (unchanged), session-but-no-row → honest empty passport (0 stamps, 0/N towns, no milestones; perk calc preserved verbatim, read stays pure). Client hardening: `usePassport` degrades to an `EMPTY_PASSPORT` on any failure so a transient error shows the "scan a QR" home, never a dead spinner. Also closed the deploy gap that exposed it: **hosted Auth had anonymous sign-ins disabled** (local `config.toml` enables it but that didn't propagate) — pushed via `supabase config push` with the production `site_url`/redirect (temp-edited for the push, local config restored). Verified on hosted: fresh anon JWT → `businesses: []`, `towns_total: 12`. | Adopted | `supabase/migrations/0015_passport_empty_for_new_patron.sql`; `app/src/patron/usePassport.js`; `tests/contract/get-my-passport.test.ts` |
| D-023 | 2026-06-07 | **Production deploy: hosted Supabase + Vercel; dblink autonomous-conninfo moved to a config table (migration 0014).** The two dblink autonomous-commit helpers (`flag_reprint_autonomous`, `otp_attempt_autonomous` — the latter is the fail-closed P1-3 OTP brute-force counter) resolved their self-connection conninfo from `current_setting('app.reprint_conninfo')`, falling back to a local `host=db … postgres/postgres` default. On hosted Supabase that GUC can't be persisted (`ALTER DATABASE/ROLE … SET` of a custom param needs superuser, which managed `postgres` is not), so PostgREST/edge connections would hit the broken local default — and the fail-closed OTP path would surface a raw dblink error instead of `OTP_INVALID`. Fix: new `resolve_autonomous_conninfo()` reads from a service-role-only `app_config` table → session GUC → local default, so local/CI keep the default (every test unchanged) and hosted inserts the row at deploy (password in a table row, never in git). Verified on hosted: OTP counter increments through its own autonomous commit. Infra: project `vrzwrnpqfpsxlrbiyzem` (us-east-1), all 14 migrations + production reference seed (region/12 towns/season/8 gates/3 milestones, zero demo businesses), 5 edge functions (stripe-webhook `--no-verify-jwt`), pg_cron rotation-sweep + gate-snapshot active, `ENVIRONMENT=production` (dev-OTP exposure stays off). Frontend: Vercel project (root `vercel.json` builds `app/` so the `@ds → ../design` alias resolves; `/c/:path*` → `/checkin.html` rewrite; SSO protection disabled for the public app). **Pending user creds**: Stripe live keys + webhook secret, Twilio (billing + SMS stay fail-closed until set). | Adopted | `supabase/migrations/0014_autonomous_conninfo.sql`; `vercel.json`; hosted project `vrzwrnpqfpsxlrbiyzem` |
| D-022 | 2026-06-07 | **Design-handoff port (patron prototype, Claude Design bundle) + `stamp_code` contracted on §2.4/§2.6.** Ported the constitution-compliant deltas from the iterated prototype: greeting summary, natively-sized hero pass (no scale hack), home check-in CTA, "closest to ready" card, completion-sorted stamps, river-watermark region card, detail hero seal/category + "Check in here", Me commitments rows, `/c/` halo burst + perk StampGrid card + wallet preview sheet. **Explicitly NOT ported** (constitution): fake camera viewport/tap-to-scan (Art. II), "ranked by verified return visits" discovery tabs (Art. I — discovery stays curated, unranked), persona/fake data, lock-screen peek (R5 A/B platform territory). Animations are transform-only — animating opacity on text makes axe measure mid-fade contrast (flaky AA failures); keyframes live in the document head (first-paint lesson from the handoff chat). `get_my_passport`/`get_business_detail` now return the registered `stamp_code` (additive §2.4/§2.6 field, D-019/D-020 standard) — stamp faces render "HRN", never a slug or derived initials (quickstart defect #4 closed on the read paths). Check-in entry 53.1/60 KB gz after adding WalletPass+Card. | Adopted | design handoff bundle (claude.ai/design); migrations 0006/0009; `contracts/api.md` §2.4/§2.6; `app/src/patron/`, `app/src/checkin/` |
| D-021 | 2026-06-07 | **Review hardening (16 findings closed, PR #1).** (a) All §5 views are `security_invoker` — direct reads run under caller RLS; definer RPCs + service_role unaffected. (b) Hot mutations serialize on advisory xact locks: `claim_passport` per phone, `redeem_perk` per (patron, perk) — kills the merge race and redemption TOCTOU; merge writes flatten `merged_into` chains so single-hop resolution stays canonical. (c) OTP brute-force lockout: 5 failed verifies kill the code (counted autonomously via the dblink pattern, fail-closed); 5 sends/phone/hour cap at the edge. (d) `STRIPE_WEBHOOK_SECRET` fails closed — no committed fallback literal. (e) Webhook dedup is claim-first (INSERT … ON CONFLICT) with release-on-failure. (f) Merge collision semantics: colliding `steer_impressions`/`milestone_unlocks` dedupe-delete (derived facts); `perk_redemptions` repoint UNCONDITIONALLY — redemptions are audit history (Art. II), and max(redeemed_at) over the unified identity is the correct D-018 reset anchor (review P2-10 evaluated, kept by design). (g) Internal fns (`gate_metrics_evaluated`, `perk_progress_count`) revoked from app roles. | Adopted | `todos/review-consolidated.md`; migrations 0001/0003/0006/0007/0009/0011/0013; `tests/integration/review-hardening.test.ts` (12 regression tests) |
